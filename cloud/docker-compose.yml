services:
  zut-cloud:
    build: ..
    image: zut-cloud:latest
    restart: unless-stopped
    # Hard box for an agent that runs model-driven shell: bounded memory,
    # no extra privileges, no privilege escalation.
    mem_limit: 3g
    mem_reservation: 1g
    security_opt:
      - no-new-privileges:true
    env_file:
      - ./cloud.env
    environment:
      HOST: 0.0.0.0
      PORT: 8787
      DATA_ROOT: /data
    volumes:
      - zut-data:/data
    expose:
      - 8787

  caddy:
    image: caddy:2-alpine
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    depends_on:
      - zut-cloud

volumes:
  zut-data:
  caddy-data:
  caddy-config:
